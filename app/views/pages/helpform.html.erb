<div class="container card-help mt-5">
  <div class="helpform">
    <p>Precisa de ajuda? Manda aí!</p>
      <%= form_with model: @form do |form| %>
        <div class="form-group">
          <%= form.text_field :nome, class: "form-control", placeholder: "Digite seu nome" %>
        </div>

        <div class="form-group">
          <%= form.text_field :empresa, class: "form-control", placeholder: "Digite o nome da empresa" %>
        </div>

        <div class="form-group">
          <%= form.email_field :email, class: "form-control", placeholder: "Digite seu e-mail" %>
        </div>

        <div class="form-group">
          <%= form.text_area :comentarios, class: "form-control", placeholder: "Digite a sua mensagem!" %>
        </div>

        <%= form.submit "Enviar", class: "btn-gradient" %>
      <% end %>
  </div>

</div>
